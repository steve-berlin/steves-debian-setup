# TODO

- `debloat-kde.sh` (currently at repo root, Spanish): organize into the
  right folder (likely `installers/`, rename to `install-` or
  `debloat-` prefix to match conventions), translate to English (US),
  bring up to repo standards (`set -euo pipefail`, `--dry-run`,
  idempotent, preflight). Borrowed from
  https://github.com/cl0v3r404/Debloat-KDE-Plasma-Debian — credit the
  upstream repo in the script header.
